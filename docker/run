FROM base

ADD wheelhouse /wheelhouse
RUN . /appenv/bin/activate; pip install --no-index -f wheelhouse testapp
CMD . /appenv/bin/activate; gunicorn "testapp.server:make_app()" --bind unix://tmp/testapp.sock
